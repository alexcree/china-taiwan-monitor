# Dashboard env (apps/dashboard). Copy to .env.local for local dev.
# In Vercel, set these via Project Settings → Environment Variables.

SUPABASE_URL=
SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
