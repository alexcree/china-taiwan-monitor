import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Scenarios — China–Taiwan Monitor" };

export default function ScenariosPage() {
  return (
    <ComingSoon
      phase="Phase 6"
      title="Rolling scenarios"
      description="Short-term scenarios tracked across briefs, with probability shifts over time. Light version free; triggers, implications, and analyst notes are newsletter-only."
    />
  );
}
