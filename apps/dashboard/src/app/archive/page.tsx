import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "Archive — China–Taiwan Monitor" };

export default function ArchivePage() {
  return (
    <ComingSoon
      phase="Phase 3"
      title="Brief archive"
      description="Every past daily brief, searchable by date, sector, and keyword. Free, with the same lighter view as today's brief."
    />
  );
}
