/**
 * Supabase client + generated types — Phase 1/2.
 *
 * Will export:
 *   - Typed Supabase client (anon and service-role variants)
 *   - Generated row types from supabase gen types
 *   - Helper queries (latestBrief, articlesLast24h, etc.)
 *
 * Schema migration lives under /supabase/migrations once the project is
 * initialized.
 */

export const _placeholder = true;
